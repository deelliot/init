11:Use the output of the previous command to find the name and IP address
of the device that makes the link between you (local network) and the
outside world

nat = network address translation, and it allows multiple devices to access the
internet through a single public address.

use nslookup 10.60.1.1 to find the ip address and name